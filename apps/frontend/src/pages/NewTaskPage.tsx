import { ArrowLeft, Info } from "lucide-react";
import type { CreateTaskInput, Task, ViewId } from "../types";
import { TaskComposer } from "../components/tasks/TaskComposer";

export function NewTaskPage({ onSubmit, submitting, onNavigate, recentTask, onOpenTask }: { onSubmit: (input: CreateTaskInput) => void; submitting: boolean; onNavigate: (view: ViewId) => void; recentTask?: Task; onOpenTask: (task: Task) => void }) { return <div className="page-stack narrow-page"><button className="back-button" onClick={() => onNavigate("overview")}><ArrowLeft size={15} /> Back to overview</button><div className="task-intro"><div><p className="eyebrow">NEW WORKFLOW</p><h2>What do you need<br /><em>done today?</em></h2><p>Give the local agent a goal. It will plan the work, select the right capability and show its reasoning path.</p></div><div className="intake-note"><Info size={16} /><span>Confidential files are processed inside your configured local environment.</span></div></div><div className="composer-layout"><TaskComposer onSubmit={onSubmit} submitting={submitting} /><aside className="task-side-note"><div className="side-note-number">01</div><h3>Start with a clear outcome</h3><p>Describe the deliverable you want, not just the question you have. The agent will decide which local tools it needs.</p><div className="side-note-divider" /><strong>Good examples</strong><ul><li>Draft an approval note from this inspection report.</li><li>Calculate the pressure drop and verify the result.</li><li>Compare this finding against our local SOPs.</li></ul>{recentTask && <button className="recent-run" onClick={() => onOpenTask(recentTask)}><span>Latest run</span><strong>{recentTask.title}</strong><small>Open execution trace <ArrowLeft size={12} /></small></button>}</aside></div></div>; }
