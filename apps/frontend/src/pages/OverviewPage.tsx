import { ArrowRight, BookOpen, CheckCircle2, Clock3, FileOutput, LockKeyhole, Network, Play, Plus, ShieldCheck, Sparkles, TerminalSquare, Zap } from "lucide-react";
import type { HealthStatus, Task, ViewId } from "../types";
import { TaskCard } from "../components/tasks/TaskCard";
import { MetricCard } from "../components/ui/MetricCard";
import { Panel } from "../components/ui/Panel";
import { SectionHeader } from "../components/ui/SectionHeader";
import { SecurityPanel } from "../components/security/SecurityPanel";
import { previewSecurity } from "../data/demoData";

export function OverviewPage({ tasks, health, onNavigate, onOpenTask }: { tasks: Task[]; health: HealthStatus; onNavigate: (view: ViewId) => void; onOpenTask: (task: Task) => void }) { return <div className="page-stack"><div className="preview-banner"><Sparkles size={16} /><span><strong>Preview workspace</strong> Some backend workflows are not connected yet. Preview data is clearly marked and will be replaced by live events during integration.</span><button onClick={() => onNavigate("settings")}>View connection details <ArrowRight size={14} /></button></div><section className="hero-grid"><div className="hero-copy"><div className="hero-kicker"><span className="kicker-line" />CONFIDENTIAL WORK, LOCALLY EXECUTED</div><h2>Good work needs<br /><em>a safe place to run.</em></h2><p>Orchestrate documents, code and local knowledge without sending sensitive work outside your infrastructure.</p><button className="primary-button" onClick={() => onNavigate("new-task")}>Start a task <ArrowRight size={16} /></button><div className="hero-trust"><LockKeyhole size={15} /><span>On-premise by design</span><i /><span>Open-weight models</span></div></div><div className="hero-visual"><div className="orbital orbital-one" /><div className="orbital orbital-two" /><div className="core-orb"><ShieldCheck size={30} /><span>LOCAL</span></div><div className="visual-node node-top"><span className="node-dot" />Local models</div><div className="visual-node node-right"><span className="node-dot" />Your documents</div><div className="visual-node node-bottom"><span className="node-dot" />Controlled tools</div></div></section><div className="metrics-grid"><MetricCard label="Completed tasks" value="24" detail="+8 this week" icon={CheckCircle2} /><MetricCard label="Knowledge indexed" value="1,248" detail="chunks across 12 docs" icon={BookOpen} tone="blue" /><MetricCard label="Avg. response time" value="18.4s" detail="Last 30 tasks" icon={Clock3} tone="amber" /><MetricCard label="External calls" value="--" detail="Measurement unavailable" icon={Network} tone="rose" /></div><div className="content-grid overview-content"><div><SectionHeader eyebrow="RECENT ACTIVITY" title="Your work" action={<button className="text-button" onClick={() => onNavigate("activity")}>View all <ArrowRight size={14} /></button>} /><div className="task-list">{tasks.slice(0, 3).map((task) => <TaskCard key={task.id} task={task} onOpen={onOpenTask} />)}</div></div><div className="right-column"><SectionHeader eyebrow="GET STARTED" title="Run a workflow" /><Panel className="workflow-callout"><div className="callout-icon"><FileOutput size={20} /></div><h3>Inspection to approval note</h3><p>Upload a scanned report, retrieve relevant SOPs and generate a review-ready DOCX.</p><button className="outline-button" onClick={() => onNavigate("new-task")}>Open workflow <Play size={14} /></button></Panel><SecurityPanel health={health} security={previewSecurity} /></div></div><Panel className="capability-strip"><div className="capability-intro"><span className="tiny-orb"><Zap size={15} /></span><div><strong>One workbench. Three capabilities.</strong><span>Choose the right local workflow for the job.</span></div></div><button onClick={() => onNavigate("new-task")}><TerminalSquare size={16} /> Coding agent <ArrowRight size={14} /></button><button onClick={() => onNavigate("new-task")}><BookOpen size={16} /> Knowledge retrieval <ArrowRight size={14} /></button><button onClick={() => onNavigate("new-task")}><Plus size={16} /> Start from scratch <ArrowRight size={14} /></button></Panel></div>; }
