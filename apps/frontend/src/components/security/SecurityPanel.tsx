import { AlertTriangle, CheckCircle2, LockKeyhole, Network, Server, ShieldCheck } from "lucide-react";
import type { HealthStatus, SecurityStatus } from "../../types";
import { Panel } from "../ui/Panel";

export function SecurityPanel({ health, security }: { health: HealthStatus; security: SecurityStatus }) { return <Panel className="security-panel"><div className="panel-heading"><div><p className="eyebrow">SOVEREIGNTY STATUS</p><h3>Inside your boundary</h3></div><LockKeyhole size={19} /></div><div className="security-summary"><div className={`security-shield ${security.measured ? "verified" : "unverified"}`}>{security.measured ? <CheckCircle2 size={25} /> : <AlertTriangle size={25} />}</div><div><strong>{security.measured ? "Evidence verified" : "Evidence unavailable"}</strong><p>{security.message}</p></div></div><div className="security-list"><div><Server size={16} /><span>Model runtime</span><strong className={health.connected ? "text-mint" : "text-muted"}>{health.connected ? "Connected" : "Unavailable"}</strong></div><div><Network size={16} /><span>External egress</span><strong>{security.outbound}</strong></div><div><ShieldCheck size={16} /><span>Code sandbox</span><strong>{security.sandbox === "isolated" ? "Isolated" : "Unavailable"}</strong></div></div><p className="security-footnote">Last checked: {security.lastChecked}</p></Panel>; }
